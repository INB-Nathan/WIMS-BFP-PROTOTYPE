# Deployment & Hosting Inventory (repo-verified)

> QA'd against the actual repository (`src/docker-compose*.yml`, `src/nginx/`,
> `.github/workflows/`, `src/AGENTS.md` security rules). Replaces the earlier generic
> boilerplate table. "Security Validation" column intentionally removed per
> review; security posture is captured under Evidence Link + Lessons Learned.

| Deployment Item | Provider / Tool | Environment Type | Free / Paid / Subscription | Purpose / Use | Configuration Details | Evidence Link / Screenshot URL | Official URL / Documentation | Cost Notes | Lessons Learned / Advice |
|---|---|---|---|---|---|---|---|---|---|
| Hosting Server | Contabo VPS | VPS | Subscription-based | Deploy production/staging system | Ubuntu, Nginx reverse proxy, Docker Compose (`src/docker-compose.yml` + `docker-compose.prod.yml`) | `src/docker-compose.yml` | https://contabo.com | Monthly cost; student credits may apply | Avoid shared hosting for active pentesting; isolate the box. |
| Domain Name | Registrar + DNS | Domain / DNS | Subscription-based | Public access URL | Cert domain is `wimsbfp.tech` (`/etc/ln/live/wimsbfp.tech/`); live DNS `wims-bfp.bfp.gov.ph`; nginx allows origin `https://wims.in` | `src/nginx/nginx.conf` (ssl cert path, `wims.in` CORS) | https://developers.cloudflare.com/dns/ | Annual domain cost | Document the REAL domain (`wimsbfp.tech`), not a placeholder registrar. Keep DNS records in the repo/notes. |
| SSL Certificate | Let's Encrypt | SSL/TLS | Free / OSS | HTTPS enforcement | Certs at `/etc/ln/live/wimsbfp.tech/{ln.pem,privkey.pem}`; mounted via `LETSENCRYPT_DIR`; HTTP→HTTPS redirect in nginx | `src/nginx/nginx.conf`; `src/.env.production.example` (`LETSENCRYPT_DIR=/etc/ln`) | https://letsencrypt.org/docs/ | Free | Document cert auto-renewal; verify the renew hook. |
| Database Hosting | Self-hosted PostgreSQL | Database Server | Self-hosted Free | Production database | `postgres` service + `postgres_data` volume; internal Docker network only (not published) | `src/docker-compose.yml` (`postgres`); `src/postgres-init/` | https://www.postgresql.org/docs/ | Depends on VPS | Do NOT expose DB port publicly; rely on compose internal network + strong creds. |
| Identity / Auth (RBAC) | Keycloak `quay.io/keycloak/keycloak:26.6.4` | Container | Free / OSS | SSO, role-based access, MFA (demo OTP) | `keycloak` + `keycloak-bootstrap` services; `KC_HOSTNAME_URL`, `KEYCLOAK_URL` env; brute-force protection via Suricata rules | `src/docker-compose.yml:75,143`; `src/AGENTS.md` (RLS/security rules) | https://www.keycloak.org/docs/ | Free | Enforce RBAC server-side; never trust frontend role checks. |
| Secrets / PII Crypto | OpenBao `openbao/openbao:2.5.5` | Container | Free / OSS | Secrets vault + AES-GCM PII encryption (at rest / in transit) | `openbao` + `openbao-bootstrap`; `OPENBAO_ADDR`; used by `services/kms/openbao_client.py` and audit-export verifier | `src/docker-compose.yml:162`; `src/backend/services/kms/openbao_client.py` | https://openbao.org/docs/ | Free | Never write plaintext PII; always go through the OpenBao/AES-GCM provider. |
| Intrusion Detection (IDS) | Suricata `wims-suricata` | Container | Free / OSS | Network IDS at the edge | `wims-suricata` service; `extra_hosts` to reach gateway; custom BFP detection rules | `src/docker-compose.yml:440`; `src/suricata/` (gap rules #155/#450) | https://suricata.io/docs/ | Free | Keep IDS rules version-pinned; review alerts regularly for defense evidence. |
| LLM / XAI | Ollama `ollama/ollama:0.30.10` | Container | Free / OSS | Local explainable-AI for incident summaries | `ollama` + `ollama-model-pull`; local-only, no external API → data residency | `src/docker-compose.yml:206` | https://ollama.com/ | Free (uses VPS CPU/GPU) | Keep model local to avoid sending PII to third-party LLM APIs. |
| Routing Engine | OSRM `osrm/osrm-backend:v5.25.0` | Container (prod) | Free / OSS | Shortest-path routing for dispatch | `osrm` service (prod only); `metro-manila.osrm`; `OSRM_BASE_URL=http://osrm:5000` | `src/docker-compose.prod.yml:6` | https://project-osrm.org/docs/ | Free | Internal-only network; prebuild the `.osrm` extract for Metro Manila. |
| Cache / Broker | Redis | Container | Free / OSS | Session cache + Celery broker | `redis` service; internal network | `src/docker-compose.yml:45` | https://redis.io/docs/ | Free | Bind to internal net; do not expose the Redis port. |
| Async Tasks | Celery `celery-worker` | Container | Free / OSS | Background jobs (exports, offline sync) | `celery-worker` service; consumes Redis broker | `src/docker-compose.yml:318` | https://docs.celeryq.dev/ | Free | Run as non-privileged app user; use service adapters for external calls. |
| Email (non-prod) | Mailhog | Container | Free / OSS | Capture outbound email in dev/staging | `mailhog` service; not for production PII | `src/docker-compose.yml:64` | https://github.com/mailhog/MailHog | Free | Disable / do not route real PII through Mailhog in prod. |
| Edge Bot Protection | nginx-ultimate-bad-bot-blocker | Nginx module | Free / OSS | Block bad bots / DDoS at the edge | `include .../blockbots.conf`, `ddos.conf`; mounted `./nginx/bot-blocker` | `src/nginx/nginx.conf:78-97`; PR #540 | https://github.com/mitchellkrogza/nginx-ultimate-bad-bot-blocker | Free | Keep blocklist updated; whitelist legit crawlers (e.g. gov monitors). |
| CI/CD Pipeline | GitHub Actions (`ci.yml`, `cd.yml`, `deploy.yml`) | SaaS | Free tier | Lint/test merge gate + auto-deploy to VPS | `make ci-local` locally; `deploy.yml` does `git reset --hard` + `docker compose up -d` on push to `master` | `.github/workflows/` | https://docs.github.com/actions/ | Free tier | Fails-closed on lint/test failure; every `master` push triggers deploy — coordinate manual VPS work with deploys. |
| Security Baseline / ASVS | OWASP ASVS L2 | Process | Free | Application security standard | ASVS compliance tracked in `../system-wiki-backup/security/` | `../system-wiki-backup/security/asvs-l2-state.json`, `asvs-l2-overrides.md` (off-repo backup) | https://owasp.org/www-project-application-security-verification-standard/ | Free | Re-run the ASVS audit after each security-relevant change. |
| Backup and Recovery | pg_dump / cloud snapshots | Backup | Free / Paid | Recover data after failure/testing | Scheduled backup + restore test (ops-managed, **not** in repo) | `src/postgres-init/` (schema source of truth) | https://www.postgresql.org/docs/current/backup.html | Storage cost may apply | Always backup before security testing; document restore drill. |
| Monitoring / Logs | Nginx logs, app logs, Suricata logs, uptime monitor | Monitoring | Free Tier / Subscription | Track errors / security events | Access logs, audit logs (append-only `system_audit_trails`), uptime checks; Suricata alerts | `src/backend/services/` (audit); `wims-suricata` logs | https://nginx.org/en/docs/ | Depends on tool | Save logs for defense evidence; retain audit trail as required record. |
