# Dependencies
node_modules/
__pycache__/
*.pyc
.pytest_cache/
.ruff_cache/
.venv/
.venv-test/
venv/
env/

# Build outputs
dist/
build/
target/
*.egg-info/
.next/
out/

# IDE / Editor
.idea/
.vscode/
*.swp
*.swo

# Secrets and local config (never commit)
.cursor/mcp.json
.env
.env.local
.env.*.local
firebase-creds.json
letsencrypt/
*.pem
*.key
*service-account*.json
src/backend/firebase-service-account.json

# Logs and runtime
*.log
*.rdb
.deploy_history
src/suricata/logs/
src/osrm/data/

# OS
.DS_Store
Thumbs.db

# Cursor / agent tooling (local only)
.cursor/
.ai-context/
.claude/
.superpowers/

# Local dev workspace (not for version control)
local-docs/

# Office temp/lock files
**/.~lock.*

# Local AFOR filled workbook (if not meant for version control)
AFORs/afor_filled.xlsx

# nathan local wiki AI tools
.codex/
system-wiki/.obsidian/
.obsidian/
.hermes/
.antigravitycli/

# Network capture artifacts (never commit — large/noisy/sensitive)
*.har

# Worktrees and local subagent artifacts
.worktrees/
.pi-subagents/

# Pi local/generated state (tracked .pi source remains visible)
.pi/sessions/
.pi/npm/
.pi/git/

# Browser QA artifacts (screenshots, traces, logs; auto-pruned by the
# wims-browser extension, but never committed)
.pi/browser/
